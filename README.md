# Budibase Multi-Component Plugin

A Budibase plugin containing multiple components, created because the free version of Budibase allows too few plugin slots.

## Included Components

- **Binding component** for reusing bindings
- **Iterator component** for executing sequential tasks
- **Poller component** for periodic polling
- **Tabs component**
